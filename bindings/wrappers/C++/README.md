# The C++-Native DSL Wrapper for `libglr`

This library is a C++ wrapper for `libglr`, separate from the SWIG-generated bindings. In fact, this library relies on the SWIG-generated bindings as the basis for accessing `libglr` functionality. This wrapper is a header-only, template-based native DSL that offers a highly abstracted interface to `libglr`. If you need raw functionality, the SWIG-generated bind
